# Production environment - Tunnix AWS Deployment
# VITE_API_URL points to the EC2 Elastic IP (HTTP, no custom domain yet).
# Update this value after running `terraform apply` and getting the EC2 IP.
#
# Example: VITE_API_URL=http://13.251.xx.xx
VITE_API_URL=http://REPLACE_WITH_EC2_ELASTIC_IP
